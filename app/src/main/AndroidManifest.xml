<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="agriculture.com.agriculture">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".activity.Extra.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.activ.SplashActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activity.activ.MainActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.activ.LoginActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.activ.SignUpActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activity.activ.DrawerActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.activ.SubListingActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.activ.ActivityForGetPassword"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.activ.ActivityConfirmOtp"
            android:launchMode="singleTop" />
        <activity
            android:name=".activity.activ.ActivityResetPassword"
            android:launchMode="singleTop"></activity>
        <activity android:name=".activity.activ.EventSubEventListingActivity"></activity>
    </application>

</manifest>